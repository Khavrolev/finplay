export enum InputType {
  Text = "text",
  Password = "password",
  Checkbox = "checkbox",
}

export enum ActionType {
  Login = "login",
  Add = "add",
  Edit = "edit",
  Delete = "delete",
  Cancel = "cancel",
}
