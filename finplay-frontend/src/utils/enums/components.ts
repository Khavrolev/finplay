export enum InputType {
  Text = "text",
  Password = "password",
}

export enum ActionPopupType {
  Add = "add",
  Edit = "edit",
  Delete = "delete",
}

export enum FormButtonType {
  Login = "login",
  Add = "add",
}
