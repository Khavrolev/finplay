export enum UserEndpoints {
  Login = "/user/login",
  Logout = "/user/logout",
  Refresh = "/user/refresh",
}

export enum GameDataEndpoints {
  GetData = "/data",
  CreateGroup = "/data/creategroup",
  UpdateGroup = "/data/updategroup",
  DeleteGroup = "/data/deletegroup",
}
